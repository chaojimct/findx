[package]
name = "findx2-ipc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "findx2 命名管道 IPC 请求/响应类型（JSON）"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
