[package]
name = "findx2-macos"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "findx2 macOS 后端：getattrlistbulk 扫描 + FSEvents 增量"

[dependencies]
findx2-core = { path = "../findx2-core" }
thiserror = "1"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2"
