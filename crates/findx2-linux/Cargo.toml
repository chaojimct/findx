[package]
name = "findx2-linux"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "findx2 Linux 后端：statx 遍历建库 + fanotify/inotify 增量"

[dependencies]
findx2-core = { path = "../findx2-core" }
thiserror = "1"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
notify = "6"
